/**
 * App.js — COHUA Ray-Ban Companion App
 *
 * Root component. Renders the main dashboard screen.
 * Future: add navigation for settings, campaign details, etc.
 */

import React from 'react';
import HomeScreen from './screens/HomeScreen';

export default function App() {
  return <HomeScreen />;
}
